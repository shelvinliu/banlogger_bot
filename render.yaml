services:
  - type: web
    name: telegram-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn bot:app --host 0.0.0.0 --port 10000"
    envVars:
      - key: TOKEN
        value: "你的TelegramBotToken"
